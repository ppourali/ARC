/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.ReceiptsAggregator;
import com.arc.model.Receipts;
import com.arc.repository.ReceiptsRepository;


@Service
public class ReceiptsServiceImpl implements ReceiptsService {
    @Autowired
    private ReceiptsRepository receiptsRepository;
    
    private ReceiptsAggregator enrich(Receipts receipts) {
        ReceiptsAggregator receiptsAggregator = null;
        
        if (receipts != null) {
            receiptsAggregator = new ReceiptsAggregator();
            
            receiptsAggregator.setReceipts(receipts);
        }
        
        return receiptsAggregator;
    }
    
    private List<ReceiptsAggregator> enrich(List<Receipts> receiptss) {
        final List<ReceiptsAggregator> receiptsAggregators = new ArrayList<>();
        
        if (receiptss != null) {
            for (Receipts receipts: receiptss) {
                receiptsAggregators.add(this.enrich(receipts));
            }
        }
        
        return receiptsAggregators;
    }
    
    @Override
    public List<ReceiptsAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<Receipts> receiptss = this.receiptsRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(receiptss);
    }
    
    @Override
    public List<ReceiptsAggregator> findAll(Connection con) {
        final List<Receipts> receiptss = this.receiptsRepository.findAll(con);
        
        return this.enrich(receiptss);
    }
    
    @Override
    public List<ReceiptsAggregator> findAll(List<String> fields_to_ignore) {
        final List<Receipts> receiptss = this.receiptsRepository.findAll(fields_to_ignore);
        
        return this.enrich(receiptss);
    }
    
    @Override
    public List<ReceiptsAggregator> findAll() {
        final List<Receipts> receiptss = this.receiptsRepository.findAll();
        
        return this.enrich(receiptss);
    }
    
    @Override
    public List<ReceiptsAggregator> findByModel(Receipts receipts, List<String> fields_to_ignore, Connection con) {
        final List<Receipts> receiptss = this.receiptsRepository.findByModel(receipts, fields_to_ignore, con);
        
        return this.enrich(receiptss);
    }
    
    @Override
    public List<ReceiptsAggregator> findByModel(Receipts receipts, Connection con) {
        final List<Receipts> receiptss = this.receiptsRepository.findByModel(receipts, con);
        
        return this.enrich(receiptss);
    }
    
    @Override
    public List<ReceiptsAggregator> findByModel(Receipts receipts, List<String> fields_to_ignore) {
        final List<Receipts> receiptss = this.receiptsRepository.findByModel(receipts, fields_to_ignore);
        
        return this.enrich(receiptss);
    }
    
    @Override
    public List<ReceiptsAggregator> findByModel(Receipts receipts) {
        final List<Receipts> receiptss = this.receiptsRepository.findByModel(receipts);
        
        return this.enrich(receiptss);
    }
    
    @Override
    public ReceiptsAggregator findById(Long id, List<String> fields_to_ignore, Connection con) {
        final Receipts receipts = this.receiptsRepository.findById(id, fields_to_ignore, con);
        
        return this.enrich(receipts);
    }
    
    @Override
    public ReceiptsAggregator findById(Long id, Connection con) {
        final Receipts receipts = this.receiptsRepository.findById(id, con);
        
        return this.enrich(receipts);
    }
    
    @Override
    public ReceiptsAggregator findById(Long id, List<String> fields_to_ignore) {
        final Receipts receipts = this.receiptsRepository.findById(id, fields_to_ignore);
        
        return this.enrich(receipts);
    }
    
    @Override
    public ReceiptsAggregator findById(Long id) {
        final Receipts receipts = this.receiptsRepository.findById(id);
        
        return this.enrich(receipts);
    }
    
    @Override
    public Receipts insert(Receipts receipts, Connection con) {
        return this.receiptsRepository.insert(receipts, con);
    }
    
    @Override
    public Receipts insert(Receipts receipts) {
        return this.receiptsRepository.insert(receipts);
    }
    
    @Override
    public Receipts update(Receipts receipts, boolean exclude_nulls, Connection con) {
        return this.receiptsRepository.update(receipts, exclude_nulls, con);
    }
    
    @Override
    public Receipts update(Receipts receipts, boolean exclude_nulls) {
        return this.receiptsRepository.update(receipts, exclude_nulls);
    }
    
    @Override
    public Receipts save(Receipts receipts, Connection con) {
        return this.receiptsRepository.save(receipts, con);
    }
    
    @Override
    public Receipts save(Receipts receipts) {
        return this.receiptsRepository.save(receipts);
    }
    
    @Override
    public void delete(Long id, Connection con) {
        this.receiptsRepository.delete(id, con);
    }
    
    @Override
    public void delete(Long id) {
        this.receiptsRepository.delete(id);
    }
    
    @Override
    public void deleteByModel(Receipts receipts, Connection con) {
        this.receiptsRepository.deleteByModel(receipts, con);
    }
    
    @Override
    public void deleteByModel(Receipts receipts) {
        this.receiptsRepository.deleteByModel(receipts);
    }
}
