/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.SampleAggregator;
import com.arc.model.Sample;
import com.arc.repository.SampleRepository;


@Service
public class SampleServiceImpl implements SampleService {
    @Autowired
    private SampleRepository sampleRepository;
    
    private SampleAggregator enrich(Sample sample) {
        SampleAggregator sampleAggregator = null;
        
        if (sample != null) {
            sampleAggregator = new SampleAggregator();
            
            sampleAggregator.setSample(sample);
        }
        
        return sampleAggregator;
    }
    
    private List<SampleAggregator> enrich(List<Sample> samples) {
        final List<SampleAggregator> sampleAggregators = new ArrayList<>();
        
        if (samples != null) {
            for (Sample sample: samples) {
                sampleAggregators.add(this.enrich(sample));
            }
        }
        
        return sampleAggregators;
    }
    
    @Override
    public List<SampleAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<Sample> samples = this.sampleRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(samples);
    }
    
    @Override
    public List<SampleAggregator> findAll(Connection con) {
        final List<Sample> samples = this.sampleRepository.findAll(con);
        
        return this.enrich(samples);
    }
    
    @Override
    public List<SampleAggregator> findAll(List<String> fields_to_ignore) {
        final List<Sample> samples = this.sampleRepository.findAll(fields_to_ignore);
        
        return this.enrich(samples);
    }
    
    @Override
    public List<SampleAggregator> findAll() {
        final List<Sample> samples = this.sampleRepository.findAll();
        
        return this.enrich(samples);
    }
    
    @Override
    public List<SampleAggregator> findByModel(Sample sample, List<String> fields_to_ignore, Connection con) {
        final List<Sample> samples = this.sampleRepository.findByModel(sample, fields_to_ignore, con);
        
        return this.enrich(samples);
    }
    
    @Override
    public List<SampleAggregator> findByModel(Sample sample, Connection con) {
        final List<Sample> samples = this.sampleRepository.findByModel(sample, con);
        
        return this.enrich(samples);
    }
    
    @Override
    public List<SampleAggregator> findByModel(Sample sample, List<String> fields_to_ignore) {
        final List<Sample> samples = this.sampleRepository.findByModel(sample, fields_to_ignore);
        
        return this.enrich(samples);
    }
    
    @Override
    public List<SampleAggregator> findByModel(Sample sample) {
        final List<Sample> samples = this.sampleRepository.findByModel(sample);
        
        return this.enrich(samples);
    }
    
    @Override
    public SampleAggregator findByContext(String context, List<String> fields_to_ignore, Connection con) {
        final Sample sample = this.sampleRepository.findByContext(context, fields_to_ignore, con);
        
        return this.enrich(sample);
    }
    
    @Override
    public SampleAggregator findByContext(String context, Connection con) {
        final Sample sample = this.sampleRepository.findByContext(context, con);
        
        return this.enrich(sample);
    }
    
    @Override
    public SampleAggregator findByContext(String context, List<String> fields_to_ignore) {
        final Sample sample = this.sampleRepository.findByContext(context, fields_to_ignore);
        
        return this.enrich(sample);
    }
    
    @Override
    public SampleAggregator findByContext(String context) {
        final Sample sample = this.sampleRepository.findByContext(context);
        
        return this.enrich(sample);
    }
    
    @Override
    public Sample insert(Sample sample, Connection con) {
        return this.sampleRepository.insert(sample, con);
    }
    
    @Override
    public Sample insert(Sample sample) {
        return this.sampleRepository.insert(sample);
    }
    
    @Override
    public Sample update(Sample sample, boolean exclude_nulls, Connection con) {
        return this.sampleRepository.update(sample, exclude_nulls, con);
    }
    
    @Override
    public Sample update(Sample sample, boolean exclude_nulls) {
        return this.sampleRepository.update(sample, exclude_nulls);
    }
    
    @Override
    public Sample save(Sample sample, Connection con) {
        return this.sampleRepository.save(sample, con);
    }
    
    @Override
    public Sample save(Sample sample) {
        return this.sampleRepository.save(sample);
    }
    
    @Override
    public void delete(String context, Connection con) {
        this.sampleRepository.delete(context, con);
    }
    
    @Override
    public void delete(String context) {
        this.sampleRepository.delete(context);
    }
    
    @Override
    public void deleteByModel(Sample sample, Connection con) {
        this.sampleRepository.deleteByModel(sample, con);
    }
    
    @Override
    public void deleteByModel(Sample sample) {
        this.sampleRepository.deleteByModel(sample);
    }
}
