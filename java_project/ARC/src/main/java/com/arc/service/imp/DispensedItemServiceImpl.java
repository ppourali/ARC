/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service.imp;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.DispensedItemAggregator;
import com.arc.model.DispensedItem;
import com.arc.repository.DispensedItemRepository;
import com.arc.service.DispensedItemService;


@Service
public class DispensedItemServiceImpl implements DispensedItemService {
    @Autowired
    private DispensedItemRepository dispensedItemRepository;
    
    private DispensedItemAggregator enrich(DispensedItem dispensedItem) {
        DispensedItemAggregator dispensedItemAggregator = null;
        
        if (dispensedItem != null) {
            dispensedItemAggregator = new DispensedItemAggregator();
            
            dispensedItemAggregator.setDispensedItem(dispensedItem);
        }
        
        return dispensedItemAggregator;
    }
    
    private List<DispensedItemAggregator> enrich(List<DispensedItem> dispensedItems) {
        final List<DispensedItemAggregator> dispensedItemAggregators = new ArrayList<>();
        
        if (dispensedItems != null) {
            for (DispensedItem dispensedItem: dispensedItems) {
                dispensedItemAggregators.add(this.enrich(dispensedItem));
            }
        }
        
        return dispensedItemAggregators;
    }
    
    @Override
    public List<DispensedItemAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<DispensedItem> dispensedItems = this.dispensedItemRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(dispensedItems);
    }
    
    @Override
    public List<DispensedItemAggregator> findAll(Connection con) {
        final List<DispensedItem> dispensedItems = this.dispensedItemRepository.findAll(con);
        
        return this.enrich(dispensedItems);
    }
    
    @Override
    public List<DispensedItemAggregator> findAll(List<String> fields_to_ignore) {
        final List<DispensedItem> dispensedItems = this.dispensedItemRepository.findAll(fields_to_ignore);
        
        return this.enrich(dispensedItems);
    }
    
    @Override
    public List<DispensedItemAggregator> findAll() {
        final List<DispensedItem> dispensedItems = this.dispensedItemRepository.findAll();
        
        return this.enrich(dispensedItems);
    }
    
    @Override
    public List<DispensedItemAggregator> findByModel(DispensedItem dispensedItem, List<String> fields_to_ignore, Connection con) {
        final List<DispensedItem> dispensedItems = this.dispensedItemRepository.findByModel(dispensedItem, fields_to_ignore, con);
        
        return this.enrich(dispensedItems);
    }
    
    @Override
    public List<DispensedItemAggregator> findByModel(DispensedItem dispensedItem, Connection con) {
        final List<DispensedItem> dispensedItems = this.dispensedItemRepository.findByModel(dispensedItem, con);
        
        return this.enrich(dispensedItems);
    }
    
    @Override
    public List<DispensedItemAggregator> findByModel(DispensedItem dispensedItem, List<String> fields_to_ignore) {
        final List<DispensedItem> dispensedItems = this.dispensedItemRepository.findByModel(dispensedItem, fields_to_ignore);
        
        return this.enrich(dispensedItems);
    }
    
    @Override
    public List<DispensedItemAggregator> findByModel(DispensedItem dispensedItem) {
        final List<DispensedItem> dispensedItems = this.dispensedItemRepository.findByModel(dispensedItem);
        
        return this.enrich(dispensedItems);
    }
    
    @Override
    public DispensedItemAggregator findByIds(Long id, String drug_name, List<String> fields_to_ignore, Connection con) {
        final DispensedItem dispensedItem = this.dispensedItemRepository.findByIds(id, drug_name, fields_to_ignore, con);
        
        return this.enrich(dispensedItem);
    }
    
    @Override
    public DispensedItemAggregator findByIds(Long id, String drug_name, Connection con) {
        final DispensedItem dispensedItem = this.dispensedItemRepository.findByIds(id, drug_name, con);
        
        return this.enrich(dispensedItem);
    }
    
    @Override
    public DispensedItemAggregator findByIds(Long id, String drug_name, List<String> fields_to_ignore) {
        final DispensedItem dispensedItem = this.dispensedItemRepository.findByIds(id, drug_name, fields_to_ignore);
        
        return this.enrich(dispensedItem);
    }
    
    @Override
    public DispensedItemAggregator findByIds(Long id, String drug_name) {
        final DispensedItem dispensedItem = this.dispensedItemRepository.findByIds(id, drug_name);
        
        return this.enrich(dispensedItem);
    }
    
    @Override
    public DispensedItem insert(DispensedItem dispensedItem, Connection con) {
        return this.dispensedItemRepository.insert(dispensedItem, con);
    }
    
    @Override
    public DispensedItem insert(DispensedItem dispensedItem) {
        return this.dispensedItemRepository.insert(dispensedItem);
    }
    
    @Override
    public DispensedItem update(DispensedItem dispensedItem, boolean exclude_nulls, Connection con) {
        return this.dispensedItemRepository.update(dispensedItem, exclude_nulls, con);
    }
    
    @Override
    public DispensedItem update(DispensedItem dispensedItem, boolean exclude_nulls) {
        return this.dispensedItemRepository.update(dispensedItem, exclude_nulls);
    }
    
    @Override
    public DispensedItem save(DispensedItem dispensedItem, Connection con) {
        return this.dispensedItemRepository.save(dispensedItem, con);
    }
    
    @Override
    public DispensedItem save(DispensedItem dispensedItem) {
        return this.dispensedItemRepository.save(dispensedItem);
    }
    
    @Override
    public void delete(Long id, String drug_name, Connection con) {
        this.dispensedItemRepository.delete(id, drug_name, con);
    }
    
    @Override
    public void delete(Long id, String drug_name) {
        this.dispensedItemRepository.delete(id, drug_name);
    }
    
    @Override
    public void deleteByModel(DispensedItem dispensedItem, Connection con) {
        this.dispensedItemRepository.deleteByModel(dispensedItem, con);
    }
    
    @Override
    public void deleteByModel(DispensedItem dispensedItem) {
        this.dispensedItemRepository.deleteByModel(dispensedItem);
    }
}
