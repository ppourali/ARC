/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.TrackingAggregator;
import com.arc.model.Tracking;
import com.arc.repository.TrackingRepository;


@Service
public class TrackingServiceImpl implements TrackingService {
    @Autowired
    private TrackingRepository trackingRepository;
    
    private TrackingAggregator enrich(Tracking tracking) {
        TrackingAggregator trackingAggregator = null;
        
        if (tracking != null) {
            trackingAggregator = new TrackingAggregator();
            
            trackingAggregator.setTracking(tracking);
        }
        
        return trackingAggregator;
    }
    
    private List<TrackingAggregator> enrich(List<Tracking> trackings) {
        final List<TrackingAggregator> trackingAggregators = new ArrayList<>();
        
        if (trackings != null) {
            for (Tracking tracking: trackings) {
                trackingAggregators.add(this.enrich(tracking));
            }
        }
        
        return trackingAggregators;
    }
    
    @Override
    public List<TrackingAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<Tracking> trackings = this.trackingRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(trackings);
    }
    
    @Override
    public List<TrackingAggregator> findAll(Connection con) {
        final List<Tracking> trackings = this.trackingRepository.findAll(con);
        
        return this.enrich(trackings);
    }
    
    @Override
    public List<TrackingAggregator> findAll(List<String> fields_to_ignore) {
        final List<Tracking> trackings = this.trackingRepository.findAll(fields_to_ignore);
        
        return this.enrich(trackings);
    }
    
    @Override
    public List<TrackingAggregator> findAll() {
        final List<Tracking> trackings = this.trackingRepository.findAll();
        
        return this.enrich(trackings);
    }
    
    @Override
    public List<TrackingAggregator> findByModel(Tracking tracking, List<String> fields_to_ignore, Connection con) {
        final List<Tracking> trackings = this.trackingRepository.findByModel(tracking, fields_to_ignore, con);
        
        return this.enrich(trackings);
    }
    
    @Override
    public List<TrackingAggregator> findByModel(Tracking tracking, Connection con) {
        final List<Tracking> trackings = this.trackingRepository.findByModel(tracking, con);
        
        return this.enrich(trackings);
    }
    
    @Override
    public List<TrackingAggregator> findByModel(Tracking tracking, List<String> fields_to_ignore) {
        final List<Tracking> trackings = this.trackingRepository.findByModel(tracking, fields_to_ignore);
        
        return this.enrich(trackings);
    }
    
    @Override
    public List<TrackingAggregator> findByModel(Tracking tracking) {
        final List<Tracking> trackings = this.trackingRepository.findByModel(tracking);
        
        return this.enrich(trackings);
    }
    
    @Override
    public TrackingAggregator findById(Long id, List<String> fields_to_ignore, Connection con) {
        final Tracking tracking = this.trackingRepository.findById(id, fields_to_ignore, con);
        
        return this.enrich(tracking);
    }
    
    @Override
    public TrackingAggregator findById(Long id, Connection con) {
        final Tracking tracking = this.trackingRepository.findById(id, con);
        
        return this.enrich(tracking);
    }
    
    @Override
    public TrackingAggregator findById(Long id, List<String> fields_to_ignore) {
        final Tracking tracking = this.trackingRepository.findById(id, fields_to_ignore);
        
        return this.enrich(tracking);
    }
    
    @Override
    public TrackingAggregator findById(Long id) {
        final Tracking tracking = this.trackingRepository.findById(id);
        
        return this.enrich(tracking);
    }
    
    @Override
    public Tracking insert(Tracking tracking, Connection con) {
        return this.trackingRepository.insert(tracking, con);
    }
    
    @Override
    public Tracking insert(Tracking tracking) {
        return this.trackingRepository.insert(tracking);
    }
    
    @Override
    public Tracking update(Tracking tracking, boolean exclude_nulls, Connection con) {
        return this.trackingRepository.update(tracking, exclude_nulls, con);
    }
    
    @Override
    public Tracking update(Tracking tracking, boolean exclude_nulls) {
        return this.trackingRepository.update(tracking, exclude_nulls);
    }
    
    @Override
    public Tracking save(Tracking tracking, Connection con) {
        return this.trackingRepository.save(tracking, con);
    }
    
    @Override
    public Tracking save(Tracking tracking) {
        return this.trackingRepository.save(tracking);
    }
    
    @Override
    public void delete(Long id, Connection con) {
        this.trackingRepository.delete(id, con);
    }
    
    @Override
    public void delete(Long id) {
        this.trackingRepository.delete(id);
    }
    
    @Override
    public void deleteByModel(Tracking tracking, Connection con) {
        this.trackingRepository.deleteByModel(tracking, con);
    }
    
    @Override
    public void deleteByModel(Tracking tracking) {
        this.trackingRepository.deleteByModel(tracking);
    }
}
