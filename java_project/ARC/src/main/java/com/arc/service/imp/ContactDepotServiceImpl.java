/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service.imp;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.ContactDepotAggregator;
import com.arc.model.ContactDepot;
import com.arc.repository.ContactDepotRepository;
import com.arc.service.ContactDepotService;


@Service
public class ContactDepotServiceImpl implements ContactDepotService {
    @Autowired
    private ContactDepotRepository contactDepotRepository;
    
    private ContactDepotAggregator enrich(ContactDepot contactDepot) {
        ContactDepotAggregator contactDepotAggregator = null;
        
        if (contactDepot != null) {
            contactDepotAggregator = new ContactDepotAggregator();
            
            contactDepotAggregator.setContactDepot(contactDepot);
        }
        
        return contactDepotAggregator;
    }
    
    private List<ContactDepotAggregator> enrich(List<ContactDepot> contactDepots) {
        final List<ContactDepotAggregator> contactDepotAggregators = new ArrayList<>();
        
        if (contactDepots != null) {
            for (ContactDepot contactDepot: contactDepots) {
                contactDepotAggregators.add(this.enrich(contactDepot));
            }
        }
        
        return contactDepotAggregators;
    }
    
    @Override
    public List<ContactDepotAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<ContactDepot> contactDepots = this.contactDepotRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(contactDepots);
    }
    
    @Override
    public List<ContactDepotAggregator> findAll(Connection con) {
        final List<ContactDepot> contactDepots = this.contactDepotRepository.findAll(con);
        
        return this.enrich(contactDepots);
    }
    
    @Override
    public List<ContactDepotAggregator> findAll(List<String> fields_to_ignore) {
        final List<ContactDepot> contactDepots = this.contactDepotRepository.findAll(fields_to_ignore);
        
        return this.enrich(contactDepots);
    }
    
    @Override
    public List<ContactDepotAggregator> findAll() {
        final List<ContactDepot> contactDepots = this.contactDepotRepository.findAll();
        
        return this.enrich(contactDepots);
    }
    
    @Override
    public List<ContactDepotAggregator> findByModel(ContactDepot contactDepot, List<String> fields_to_ignore, Connection con) {
        final List<ContactDepot> contactDepots = this.contactDepotRepository.findByModel(contactDepot, fields_to_ignore, con);
        
        return this.enrich(contactDepots);
    }
    
    @Override
    public List<ContactDepotAggregator> findByModel(ContactDepot contactDepot, Connection con) {
        final List<ContactDepot> contactDepots = this.contactDepotRepository.findByModel(contactDepot, con);
        
        return this.enrich(contactDepots);
    }
    
    @Override
    public List<ContactDepotAggregator> findByModel(ContactDepot contactDepot, List<String> fields_to_ignore) {
        final List<ContactDepot> contactDepots = this.contactDepotRepository.findByModel(contactDepot, fields_to_ignore);
        
        return this.enrich(contactDepots);
    }
    
    @Override
    public List<ContactDepotAggregator> findByModel(ContactDepot contactDepot) {
        final List<ContactDepot> contactDepots = this.contactDepotRepository.findByModel(contactDepot);
        
        return this.enrich(contactDepots);
    }
    
    @Override
    public ContactDepotAggregator findByIds(String contact_id, String drug_name, List<String> fields_to_ignore, Connection con) {
        final ContactDepot contactDepot = this.contactDepotRepository.findByIds(contact_id, drug_name, fields_to_ignore, con);
        
        return this.enrich(contactDepot);
    }
    
    @Override
    public ContactDepotAggregator findByIds(String contact_id, String drug_name, Connection con) {
        final ContactDepot contactDepot = this.contactDepotRepository.findByIds(contact_id, drug_name, con);
        
        return this.enrich(contactDepot);
    }
    
    @Override
    public ContactDepotAggregator findByIds(String contact_id, String drug_name, List<String> fields_to_ignore) {
        final ContactDepot contactDepot = this.contactDepotRepository.findByIds(contact_id, drug_name, fields_to_ignore);
        
        return this.enrich(contactDepot);
    }
    
    @Override
    public ContactDepotAggregator findByIds(String contact_id, String drug_name) {
        final ContactDepot contactDepot = this.contactDepotRepository.findByIds(contact_id, drug_name);
        
        return this.enrich(contactDepot);
    }
    
    @Override
    public ContactDepot insert(ContactDepot contactDepot, Connection con) {
        return this.contactDepotRepository.insert(contactDepot, con);
    }
    
    @Override
    public ContactDepot insert(ContactDepot contactDepot) {
        return this.contactDepotRepository.insert(contactDepot);
    }
    
    @Override
    public ContactDepot update(ContactDepot contactDepot, boolean exclude_nulls, Connection con) {
        return this.contactDepotRepository.update(contactDepot, exclude_nulls, con);
    }
    
    @Override
    public ContactDepot update(ContactDepot contactDepot, boolean exclude_nulls) {
        return this.contactDepotRepository.update(contactDepot, exclude_nulls);
    }
    
    @Override
    public ContactDepot save(ContactDepot contactDepot, Connection con) {
        return this.contactDepotRepository.save(contactDepot, con);
    }
    
    @Override
    public ContactDepot save(ContactDepot contactDepot) {
        return this.contactDepotRepository.save(contactDepot);
    }
    
    @Override
    public void delete(String contact_id, String drug_name, Connection con) {
        this.contactDepotRepository.delete(contact_id, drug_name, con);
    }
    
    @Override
    public void delete(String contact_id, String drug_name) {
        this.contactDepotRepository.delete(contact_id, drug_name);
    }
    
    @Override
    public void deleteByModel(ContactDepot contactDepot, Connection con) {
        this.contactDepotRepository.deleteByModel(contactDepot, con);
    }
    
    @Override
    public void deleteByModel(ContactDepot contactDepot) {
        this.contactDepotRepository.deleteByModel(contactDepot);
    }
}
