/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service.imp;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.MapAAggregator;
import com.arc.model.MapA;
import com.arc.repository.MapARepository;
import com.arc.service.MapAService;


@Service
public class MapAServiceImpl implements MapAService {
    @Autowired
    private MapARepository mapARepository;
    
    private MapAAggregator enrich(MapA mapA) {
        MapAAggregator mapAAggregator = null;
        
        if (mapA != null) {
            mapAAggregator = new MapAAggregator();
            
            mapAAggregator.setMapA(mapA);
        }
        
        return mapAAggregator;
    }
    
    private List<MapAAggregator> enrich(List<MapA> mapAs) {
        final List<MapAAggregator> mapAAggregators = new ArrayList<>();
        
        if (mapAs != null) {
            for (MapA mapA: mapAs) {
                mapAAggregators.add(this.enrich(mapA));
            }
        }
        
        return mapAAggregators;
    }
    
    @Override
    public List<MapAAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<MapA> mapAs = this.mapARepository.findAll(fields_to_ignore, con);
        
        return this.enrich(mapAs);
    }
    
    @Override
    public List<MapAAggregator> findAll(Connection con) {
        final List<MapA> mapAs = this.mapARepository.findAll(con);
        
        return this.enrich(mapAs);
    }
    
    @Override
    public List<MapAAggregator> findAll(List<String> fields_to_ignore) {
        final List<MapA> mapAs = this.mapARepository.findAll(fields_to_ignore);
        
        return this.enrich(mapAs);
    }
    
    @Override
    public List<MapAAggregator> findAll() {
        final List<MapA> mapAs = this.mapARepository.findAll();
        
        return this.enrich(mapAs);
    }
    
    @Override
    public List<MapAAggregator> findByModel(MapA mapA, List<String> fields_to_ignore, Connection con) {
        final List<MapA> mapAs = this.mapARepository.findByModel(mapA, fields_to_ignore, con);
        
        return this.enrich(mapAs);
    }
    
    @Override
    public List<MapAAggregator> findByModel(MapA mapA, Connection con) {
        final List<MapA> mapAs = this.mapARepository.findByModel(mapA, con);
        
        return this.enrich(mapAs);
    }
    
    @Override
    public List<MapAAggregator> findByModel(MapA mapA, List<String> fields_to_ignore) {
        final List<MapA> mapAs = this.mapARepository.findByModel(mapA, fields_to_ignore);
        
        return this.enrich(mapAs);
    }
    
    @Override
    public List<MapAAggregator> findByModel(MapA mapA) {
        final List<MapA> mapAs = this.mapARepository.findByModel(mapA);
        
        return this.enrich(mapAs);
    }
    
    @Override
    public MapAAggregator findByCode(Long code, List<String> fields_to_ignore, Connection con) {
        final MapA mapA = this.mapARepository.findByCode(code, fields_to_ignore, con);
        
        return this.enrich(mapA);
    }
    
    @Override
    public MapAAggregator findByCode(Long code, Connection con) {
        final MapA mapA = this.mapARepository.findByCode(code, con);
        
        return this.enrich(mapA);
    }
    
    @Override
    public MapAAggregator findByCode(Long code, List<String> fields_to_ignore) {
        final MapA mapA = this.mapARepository.findByCode(code, fields_to_ignore);
        
        return this.enrich(mapA);
    }
    
    @Override
    public MapAAggregator findByCode(Long code) {
        final MapA mapA = this.mapARepository.findByCode(code);
        
        return this.enrich(mapA);
    }
    
    @Override
    public MapA insert(MapA mapA, Connection con) {
        return this.mapARepository.insert(mapA, con);
    }
    
    @Override
    public MapA insert(MapA mapA) {
        return this.mapARepository.insert(mapA);
    }
    
    @Override
    public MapA update(MapA mapA, boolean exclude_nulls, Connection con) {
        return this.mapARepository.update(mapA, exclude_nulls, con);
    }
    
    @Override
    public MapA update(MapA mapA, boolean exclude_nulls) {
        return this.mapARepository.update(mapA, exclude_nulls);
    }
    
    @Override
    public MapA save(MapA mapA, Connection con) {
        return this.mapARepository.save(mapA, con);
    }
    
    @Override
    public MapA save(MapA mapA) {
        return this.mapARepository.save(mapA);
    }
    
    @Override
    public void delete(Long code, Connection con) {
        this.mapARepository.delete(code, con);
    }
    
    @Override
    public void delete(Long code) {
        this.mapARepository.delete(code);
    }
    
    @Override
    public void deleteByModel(MapA mapA, Connection con) {
        this.mapARepository.deleteByModel(mapA, con);
    }
    
    @Override
    public void deleteByModel(MapA mapA) {
        this.mapARepository.deleteByModel(mapA);
    }
}
