/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service.imp;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.NurseDepotAggregator;
import com.arc.model.NurseDepot;
import com.arc.repository.NurseDepotRepository;
import com.arc.service.NurseDepotService;


@Service
public class NurseDepotServiceImpl implements NurseDepotService {
    @Autowired
    private NurseDepotRepository nurseDepotRepository;
    
    private NurseDepotAggregator enrich(NurseDepot nurseDepot) {
        NurseDepotAggregator nurseDepotAggregator = null;
        
        if (nurseDepot != null) {
            nurseDepotAggregator = new NurseDepotAggregator();
            
            nurseDepotAggregator.setNurseDepot(nurseDepot);
        }
        
        return nurseDepotAggregator;
    }
    
    private List<NurseDepotAggregator> enrich(List<NurseDepot> nurseDepots) {
        final List<NurseDepotAggregator> nurseDepotAggregators = new ArrayList<>();
        
        if (nurseDepots != null) {
            for (NurseDepot nurseDepot: nurseDepots) {
                nurseDepotAggregators.add(this.enrich(nurseDepot));
            }
        }
        
        return nurseDepotAggregators;
    }
    
    @Override
    public List<NurseDepotAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<NurseDepot> nurseDepots = this.nurseDepotRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(nurseDepots);
    }
    
    @Override
    public List<NurseDepotAggregator> findAll(Connection con) {
        final List<NurseDepot> nurseDepots = this.nurseDepotRepository.findAll(con);
        
        return this.enrich(nurseDepots);
    }
    
    @Override
    public List<NurseDepotAggregator> findAll(List<String> fields_to_ignore) {
        final List<NurseDepot> nurseDepots = this.nurseDepotRepository.findAll(fields_to_ignore);
        
        return this.enrich(nurseDepots);
    }
    
    @Override
    public List<NurseDepotAggregator> findAll() {
        final List<NurseDepot> nurseDepots = this.nurseDepotRepository.findAll();
        
        return this.enrich(nurseDepots);
    }
    
    @Override
    public List<NurseDepotAggregator> findByModel(NurseDepot nurseDepot, List<String> fields_to_ignore, Connection con) {
        final List<NurseDepot> nurseDepots = this.nurseDepotRepository.findByModel(nurseDepot, fields_to_ignore, con);
        
        return this.enrich(nurseDepots);
    }
    
    @Override
    public List<NurseDepotAggregator> findByModel(NurseDepot nurseDepot, Connection con) {
        final List<NurseDepot> nurseDepots = this.nurseDepotRepository.findByModel(nurseDepot, con);
        
        return this.enrich(nurseDepots);
    }
    
    @Override
    public List<NurseDepotAggregator> findByModel(NurseDepot nurseDepot, List<String> fields_to_ignore) {
        final List<NurseDepot> nurseDepots = this.nurseDepotRepository.findByModel(nurseDepot, fields_to_ignore);
        
        return this.enrich(nurseDepots);
    }
    
    @Override
    public List<NurseDepotAggregator> findByModel(NurseDepot nurseDepot) {
        final List<NurseDepot> nurseDepots = this.nurseDepotRepository.findByModel(nurseDepot);
        
        return this.enrich(nurseDepots);
    }
    
    @Override
    public NurseDepotAggregator findByDrug_name(String drug_name, List<String> fields_to_ignore, Connection con) {
        final NurseDepot nurseDepot = this.nurseDepotRepository.findByDrug_name(drug_name, fields_to_ignore, con);
        
        return this.enrich(nurseDepot);
    }
    
    @Override
    public NurseDepotAggregator findByDrug_name(String drug_name, Connection con) {
        final NurseDepot nurseDepot = this.nurseDepotRepository.findByDrug_name(drug_name, con);
        
        return this.enrich(nurseDepot);
    }
    
    @Override
    public NurseDepotAggregator findByDrug_name(String drug_name, List<String> fields_to_ignore) {
        final NurseDepot nurseDepot = this.nurseDepotRepository.findByDrug_name(drug_name, fields_to_ignore);
        
        return this.enrich(nurseDepot);
    }
    
    @Override
    public NurseDepotAggregator findByDrug_name(String drug_name) {
        final NurseDepot nurseDepot = this.nurseDepotRepository.findByDrug_name(drug_name);
        
        return this.enrich(nurseDepot);
    }
    
    @Override
    public NurseDepot insert(NurseDepot nurseDepot, Connection con) {
        return this.nurseDepotRepository.insert(nurseDepot, con);
    }
    
    @Override
    public NurseDepot insert(NurseDepot nurseDepot) {
        return this.nurseDepotRepository.insert(nurseDepot);
    }
    
    @Override
    public NurseDepot update(NurseDepot nurseDepot, boolean exclude_nulls, Connection con) {
        return this.nurseDepotRepository.update(nurseDepot, exclude_nulls, con);
    }
    
    @Override
    public NurseDepot update(NurseDepot nurseDepot, boolean exclude_nulls) {
        return this.nurseDepotRepository.update(nurseDepot, exclude_nulls);
    }
    
    @Override
    public NurseDepot save(NurseDepot nurseDepot, Connection con) {
        return this.nurseDepotRepository.save(nurseDepot, con);
    }
    
    @Override
    public NurseDepot save(NurseDepot nurseDepot) {
        return this.nurseDepotRepository.save(nurseDepot);
    }
    
    @Override
    public void delete(String drug_name, Connection con) {
        this.nurseDepotRepository.delete(drug_name, con);
    }
    
    @Override
    public void delete(String drug_name) {
        this.nurseDepotRepository.delete(drug_name);
    }
    
    @Override
    public void deleteByModel(NurseDepot nurseDepot, Connection con) {
        this.nurseDepotRepository.deleteByModel(nurseDepot, con);
    }
    
    @Override
    public void deleteByModel(NurseDepot nurseDepot) {
        this.nurseDepotRepository.deleteByModel(nurseDepot);
    }
}
