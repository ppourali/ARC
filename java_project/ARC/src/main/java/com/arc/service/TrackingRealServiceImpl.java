/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.TrackingRealAggregator;
import com.arc.model.TrackingReal;
import com.arc.repository.TrackingRealRepository;


@Service
public class TrackingRealServiceImpl implements TrackingRealService {
    @Autowired
    private TrackingRealRepository trackingRealRepository;
    
    private TrackingRealAggregator enrich(TrackingReal trackingReal) {
        TrackingRealAggregator trackingRealAggregator = null;
        
        if (trackingReal != null) {
            trackingRealAggregator = new TrackingRealAggregator();
            
            trackingRealAggregator.setTrackingReal(trackingReal);
        }
        
        return trackingRealAggregator;
    }
    
    private List<TrackingRealAggregator> enrich(List<TrackingReal> trackingReals) {
        final List<TrackingRealAggregator> trackingRealAggregators = new ArrayList<>();
        
        if (trackingReals != null) {
            for (TrackingReal trackingReal: trackingReals) {
                trackingRealAggregators.add(this.enrich(trackingReal));
            }
        }
        
        return trackingRealAggregators;
    }
    
    @Override
    public List<TrackingRealAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<TrackingReal> trackingReals = this.trackingRealRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(trackingReals);
    }
    
    @Override
    public List<TrackingRealAggregator> findAll(Connection con) {
        final List<TrackingReal> trackingReals = this.trackingRealRepository.findAll(con);
        
        return this.enrich(trackingReals);
    }
    
    @Override
    public List<TrackingRealAggregator> findAll(List<String> fields_to_ignore) {
        final List<TrackingReal> trackingReals = this.trackingRealRepository.findAll(fields_to_ignore);
        
        return this.enrich(trackingReals);
    }
    
    @Override
    public List<TrackingRealAggregator> findAll() {
        final List<TrackingReal> trackingReals = this.trackingRealRepository.findAll();
        
        return this.enrich(trackingReals);
    }
    
    @Override
    public List<TrackingRealAggregator> findByModel(TrackingReal trackingReal, List<String> fields_to_ignore, Connection con) {
        final List<TrackingReal> trackingReals = this.trackingRealRepository.findByModel(trackingReal, fields_to_ignore, con);
        
        return this.enrich(trackingReals);
    }
    
    @Override
    public List<TrackingRealAggregator> findByModel(TrackingReal trackingReal, Connection con) {
        final List<TrackingReal> trackingReals = this.trackingRealRepository.findByModel(trackingReal, con);
        
        return this.enrich(trackingReals);
    }
    
    @Override
    public List<TrackingRealAggregator> findByModel(TrackingReal trackingReal, List<String> fields_to_ignore) {
        final List<TrackingReal> trackingReals = this.trackingRealRepository.findByModel(trackingReal, fields_to_ignore);
        
        return this.enrich(trackingReals);
    }
    
    @Override
    public List<TrackingRealAggregator> findByModel(TrackingReal trackingReal) {
        final List<TrackingReal> trackingReals = this.trackingRealRepository.findByModel(trackingReal);
        
        return this.enrich(trackingReals);
    }
    
    @Override
    public TrackingRealAggregator findById(Long id, List<String> fields_to_ignore, Connection con) {
        final TrackingReal trackingReal = this.trackingRealRepository.findById(id, fields_to_ignore, con);
        
        return this.enrich(trackingReal);
    }
    
    @Override
    public TrackingRealAggregator findById(Long id, Connection con) {
        final TrackingReal trackingReal = this.trackingRealRepository.findById(id, con);
        
        return this.enrich(trackingReal);
    }
    
    @Override
    public TrackingRealAggregator findById(Long id, List<String> fields_to_ignore) {
        final TrackingReal trackingReal = this.trackingRealRepository.findById(id, fields_to_ignore);
        
        return this.enrich(trackingReal);
    }
    
    @Override
    public TrackingRealAggregator findById(Long id) {
        final TrackingReal trackingReal = this.trackingRealRepository.findById(id);
        
        return this.enrich(trackingReal);
    }
    
    @Override
    public TrackingReal insert(TrackingReal trackingReal, Connection con) {
        return this.trackingRealRepository.insert(trackingReal, con);
    }
    
    @Override
    public TrackingReal insert(TrackingReal trackingReal) {
        return this.trackingRealRepository.insert(trackingReal);
    }
    
    @Override
    public TrackingReal update(TrackingReal trackingReal, boolean exclude_nulls, Connection con) {
        return this.trackingRealRepository.update(trackingReal, exclude_nulls, con);
    }
    
    @Override
    public TrackingReal update(TrackingReal trackingReal, boolean exclude_nulls) {
        return this.trackingRealRepository.update(trackingReal, exclude_nulls);
    }
    
    @Override
    public TrackingReal save(TrackingReal trackingReal, Connection con) {
        return this.trackingRealRepository.save(trackingReal, con);
    }
    
    @Override
    public TrackingReal save(TrackingReal trackingReal) {
        return this.trackingRealRepository.save(trackingReal);
    }
    
    @Override
    public void delete(Long id, Connection con) {
        this.trackingRealRepository.delete(id, con);
    }
    
    @Override
    public void delete(Long id) {
        this.trackingRealRepository.delete(id);
    }
    
    @Override
    public void deleteByModel(TrackingReal trackingReal, Connection con) {
        this.trackingRealRepository.deleteByModel(trackingReal, con);
    }
    
    @Override
    public void deleteByModel(TrackingReal trackingReal) {
        this.trackingRealRepository.deleteByModel(trackingReal);
    }
}
