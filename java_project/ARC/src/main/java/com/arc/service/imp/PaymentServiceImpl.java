/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service.imp;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.PaymentAggregator;
import com.arc.model.Payment;
import com.arc.repository.PaymentRepository;
import com.arc.service.PaymentService;


@Service
public class PaymentServiceImpl implements PaymentService {
    @Autowired
    private PaymentRepository paymentRepository;
    
    private PaymentAggregator enrich(Payment payment) {
        PaymentAggregator paymentAggregator = null;
        
        if (payment != null) {
            paymentAggregator = new PaymentAggregator();
            
            paymentAggregator.setPayment(payment);
        }
        
        return paymentAggregator;
    }
    
    private List<PaymentAggregator> enrich(List<Payment> payments) {
        final List<PaymentAggregator> paymentAggregators = new ArrayList<>();
        
        if (payments != null) {
            for (Payment payment: payments) {
                paymentAggregators.add(this.enrich(payment));
            }
        }
        
        return paymentAggregators;
    }
    
    @Override
    public List<PaymentAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<Payment> payments = this.paymentRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(payments);
    }
    
    @Override
    public List<PaymentAggregator> findAll(Connection con) {
        final List<Payment> payments = this.paymentRepository.findAll(con);
        
        return this.enrich(payments);
    }
    
    @Override
    public List<PaymentAggregator> findAll(List<String> fields_to_ignore) {
        final List<Payment> payments = this.paymentRepository.findAll(fields_to_ignore);
        
        return this.enrich(payments);
    }
    
    @Override
    public List<PaymentAggregator> findAll() {
        final List<Payment> payments = this.paymentRepository.findAll();
        
        return this.enrich(payments);
    }
    
    @Override
    public List<PaymentAggregator> findByModel(Payment payment, List<String> fields_to_ignore, Connection con) {
        final List<Payment> payments = this.paymentRepository.findByModel(payment, fields_to_ignore, con);
        
        return this.enrich(payments);
    }
    
    @Override
    public List<PaymentAggregator> findByModel(Payment payment, Connection con) {
        final List<Payment> payments = this.paymentRepository.findByModel(payment, con);
        
        return this.enrich(payments);
    }
    
    @Override
    public List<PaymentAggregator> findByModel(Payment payment, List<String> fields_to_ignore) {
        final List<Payment> payments = this.paymentRepository.findByModel(payment, fields_to_ignore);
        
        return this.enrich(payments);
    }
    
    @Override
    public List<PaymentAggregator> findByModel(Payment payment) {
        final List<Payment> payments = this.paymentRepository.findByModel(payment);
        
        return this.enrich(payments);
    }
    
    @Override
    public PaymentAggregator findById(String id, List<String> fields_to_ignore, Connection con) {
        final Payment payment = this.paymentRepository.findById(id, fields_to_ignore, con);
        
        return this.enrich(payment);
    }
    
    @Override
    public PaymentAggregator findById(String id, Connection con) {
        final Payment payment = this.paymentRepository.findById(id, con);
        
        return this.enrich(payment);
    }
    
    @Override
    public PaymentAggregator findById(String id, List<String> fields_to_ignore) {
        final Payment payment = this.paymentRepository.findById(id, fields_to_ignore);
        
        return this.enrich(payment);
    }
    
    @Override
    public PaymentAggregator findById(String id) {
        final Payment payment = this.paymentRepository.findById(id);
        
        return this.enrich(payment);
    }
    
    @Override
    public Payment insert(Payment payment, Connection con) {
        return this.paymentRepository.insert(payment, con);
    }
    
    @Override
    public Payment insert(Payment payment) {
        return this.paymentRepository.insert(payment);
    }
    
    @Override
    public Payment update(Payment payment, boolean exclude_nulls, Connection con) {
        return this.paymentRepository.update(payment, exclude_nulls, con);
    }
    
    @Override
    public Payment update(Payment payment, boolean exclude_nulls) {
        return this.paymentRepository.update(payment, exclude_nulls);
    }
    
    @Override
    public Payment save(Payment payment, Connection con) {
        return this.paymentRepository.save(payment, con);
    }
    
    @Override
    public Payment save(Payment payment) {
        return this.paymentRepository.save(payment);
    }
    
    @Override
    public void delete(String id, Connection con) {
        this.paymentRepository.delete(id, con);
    }
    
    @Override
    public void delete(String id) {
        this.paymentRepository.delete(id);
    }
    
    @Override
    public void deleteByModel(Payment payment, Connection con) {
        this.paymentRepository.deleteByModel(payment, con);
    }
    
    @Override
    public void deleteByModel(Payment payment) {
        this.paymentRepository.deleteByModel(payment);
    }
}
