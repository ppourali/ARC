/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service.imp;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.BlackListAggregator;
import com.arc.model.BlackList;
import com.arc.repository.BlackListRepository;
import com.arc.service.BlackListService;


@Service
public class BlackListServiceImpl implements BlackListService {
    @Autowired
    private BlackListRepository blackListRepository;
    
    private BlackListAggregator enrich(BlackList blackList) {
        BlackListAggregator blackListAggregator = null;
        
        if (blackList != null) {
            blackListAggregator = new BlackListAggregator();
            
            blackListAggregator.setBlackList(blackList);
        }
        
        return blackListAggregator;
    }
    
    private List<BlackListAggregator> enrich(List<BlackList> blackLists) {
        final List<BlackListAggregator> blackListAggregators = new ArrayList<>();
        
        if (blackLists != null) {
            for (BlackList blackList: blackLists) {
                blackListAggregators.add(this.enrich(blackList));
            }
        }
        
        return blackListAggregators;
    }
    
    @Override
    public List<BlackListAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<BlackList> blackLists = this.blackListRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(blackLists);
    }
    
    @Override
    public List<BlackListAggregator> findAll(Connection con) {
        final List<BlackList> blackLists = this.blackListRepository.findAll(con);
        
        return this.enrich(blackLists);
    }
    
    @Override
    public List<BlackListAggregator> findAll(List<String> fields_to_ignore) {
        final List<BlackList> blackLists = this.blackListRepository.findAll(fields_to_ignore);
        
        return this.enrich(blackLists);
    }
    
    @Override
    public List<BlackListAggregator> findAll() {
        final List<BlackList> blackLists = this.blackListRepository.findAll();
        
        return this.enrich(blackLists);
    }
    
    @Override
    public List<BlackListAggregator> findByModel(BlackList blackList, List<String> fields_to_ignore, Connection con) {
        final List<BlackList> blackLists = this.blackListRepository.findByModel(blackList, fields_to_ignore, con);
        
        return this.enrich(blackLists);
    }
    
    @Override
    public List<BlackListAggregator> findByModel(BlackList blackList, Connection con) {
        final List<BlackList> blackLists = this.blackListRepository.findByModel(blackList, con);
        
        return this.enrich(blackLists);
    }
    
    @Override
    public List<BlackListAggregator> findByModel(BlackList blackList, List<String> fields_to_ignore) {
        final List<BlackList> blackLists = this.blackListRepository.findByModel(blackList, fields_to_ignore);
        
        return this.enrich(blackLists);
    }
    
    @Override
    public List<BlackListAggregator> findByModel(BlackList blackList) {
        final List<BlackList> blackLists = this.blackListRepository.findByModel(blackList);
        
        return this.enrich(blackLists);
    }
    
    @Override
    public BlackListAggregator findById(Long id, List<String> fields_to_ignore, Connection con) {
        final BlackList blackList = this.blackListRepository.findById(id, fields_to_ignore, con);
        
        return this.enrich(blackList);
    }
    
    @Override
    public BlackListAggregator findById(Long id, Connection con) {
        final BlackList blackList = this.blackListRepository.findById(id, con);
        
        return this.enrich(blackList);
    }
    
    @Override
    public BlackListAggregator findById(Long id, List<String> fields_to_ignore) {
        final BlackList blackList = this.blackListRepository.findById(id, fields_to_ignore);
        
        return this.enrich(blackList);
    }
    
    @Override
    public BlackListAggregator findById(Long id) {
        final BlackList blackList = this.blackListRepository.findById(id);
        
        return this.enrich(blackList);
    }
    
    @Override
    public BlackList insert(BlackList blackList, Connection con) {
        return this.blackListRepository.insert(blackList, con);
    }
    
    @Override
    public BlackList insert(BlackList blackList) {
        return this.blackListRepository.insert(blackList);
    }
    
    @Override
    public BlackList update(BlackList blackList, boolean exclude_nulls, Connection con) {
        return this.blackListRepository.update(blackList, exclude_nulls, con);
    }
    
    @Override
    public BlackList update(BlackList blackList, boolean exclude_nulls) {
        return this.blackListRepository.update(blackList, exclude_nulls);
    }
    
    @Override
    public BlackList save(BlackList blackList, Connection con) {
        return this.blackListRepository.save(blackList, con);
    }
    
    @Override
    public BlackList save(BlackList blackList) {
        return this.blackListRepository.save(blackList);
    }
    
    @Override
    public void delete(Long id, Connection con) {
        this.blackListRepository.delete(id, con);
    }
    
    @Override
    public void delete(Long id) {
        this.blackListRepository.delete(id);
    }
    
    @Override
    public void deleteByModel(BlackList blackList, Connection con) {
        this.blackListRepository.deleteByModel(blackList, con);
    }
    
    @Override
    public void deleteByModel(BlackList blackList) {
        this.blackListRepository.deleteByModel(blackList);
    }
}
