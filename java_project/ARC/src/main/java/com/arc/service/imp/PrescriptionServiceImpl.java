/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service.imp;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.PrescriptionAggregator;
import com.arc.model.Prescription;
import com.arc.repository.PrescriptionRepository;
import com.arc.service.PrescriptionService;


@Service
public class PrescriptionServiceImpl implements PrescriptionService {
    @Autowired
    private PrescriptionRepository prescriptionRepository;
    
    private PrescriptionAggregator enrich(Prescription prescription) {
        PrescriptionAggregator prescriptionAggregator = null;
        
        if (prescription != null) {
            prescriptionAggregator = new PrescriptionAggregator();
            
            prescriptionAggregator.setPrescription(prescription);
        }
        
        return prescriptionAggregator;
    }
    
    private List<PrescriptionAggregator> enrich(List<Prescription> prescriptions) {
        final List<PrescriptionAggregator> prescriptionAggregators = new ArrayList<>();
        
        if (prescriptions != null) {
            for (Prescription prescription: prescriptions) {
                prescriptionAggregators.add(this.enrich(prescription));
            }
        }
        
        return prescriptionAggregators;
    }
    
    @Override
    public List<PrescriptionAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<Prescription> prescriptions = this.prescriptionRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(prescriptions);
    }
    
    @Override
    public List<PrescriptionAggregator> findAll(Connection con) {
        final List<Prescription> prescriptions = this.prescriptionRepository.findAll(con);
        
        return this.enrich(prescriptions);
    }
    
    @Override
    public List<PrescriptionAggregator> findAll(List<String> fields_to_ignore) {
        final List<Prescription> prescriptions = this.prescriptionRepository.findAll(fields_to_ignore);
        
        return this.enrich(prescriptions);
    }
    
    @Override
    public List<PrescriptionAggregator> findAll() {
        final List<Prescription> prescriptions = this.prescriptionRepository.findAll();
        
        return this.enrich(prescriptions);
    }
    
    @Override
    public List<PrescriptionAggregator> findByModel(Prescription prescription, List<String> fields_to_ignore, Connection con) {
        final List<Prescription> prescriptions = this.prescriptionRepository.findByModel(prescription, fields_to_ignore, con);
        
        return this.enrich(prescriptions);
    }
    
    @Override
    public List<PrescriptionAggregator> findByModel(Prescription prescription, Connection con) {
        final List<Prescription> prescriptions = this.prescriptionRepository.findByModel(prescription, con);
        
        return this.enrich(prescriptions);
    }
    
    @Override
    public List<PrescriptionAggregator> findByModel(Prescription prescription, List<String> fields_to_ignore) {
        final List<Prescription> prescriptions = this.prescriptionRepository.findByModel(prescription, fields_to_ignore);
        
        return this.enrich(prescriptions);
    }
    
    @Override
    public List<PrescriptionAggregator> findByModel(Prescription prescription) {
        final List<Prescription> prescriptions = this.prescriptionRepository.findByModel(prescription);
        
        return this.enrich(prescriptions);
    }
    
    @Override
    public PrescriptionAggregator findByCode(Long code, List<String> fields_to_ignore, Connection con) {
        final Prescription prescription = this.prescriptionRepository.findByCode(code, fields_to_ignore, con);
        
        return this.enrich(prescription);
    }
    
    @Override
    public PrescriptionAggregator findByCode(Long code, Connection con) {
        final Prescription prescription = this.prescriptionRepository.findByCode(code, con);
        
        return this.enrich(prescription);
    }
    
    @Override
    public PrescriptionAggregator findByCode(Long code, List<String> fields_to_ignore) {
        final Prescription prescription = this.prescriptionRepository.findByCode(code, fields_to_ignore);
        
        return this.enrich(prescription);
    }
    
    @Override
    public PrescriptionAggregator findByCode(Long code) {
        final Prescription prescription = this.prescriptionRepository.findByCode(code);
        
        return this.enrich(prescription);
    }
    
    @Override
    public Prescription insert(Prescription prescription, Connection con) {
        return this.prescriptionRepository.insert(prescription, con);
    }
    
    @Override
    public Prescription insert(Prescription prescription) {
        return this.prescriptionRepository.insert(prescription);
    }
    
    @Override
    public Prescription update(Prescription prescription, boolean exclude_nulls, Connection con) {
        return this.prescriptionRepository.update(prescription, exclude_nulls, con);
    }
    
    @Override
    public Prescription update(Prescription prescription, boolean exclude_nulls) {
        return this.prescriptionRepository.update(prescription, exclude_nulls);
    }
    
    @Override
    public Prescription save(Prescription prescription, Connection con) {
        return this.prescriptionRepository.save(prescription, con);
    }
    
    @Override
    public Prescription save(Prescription prescription) {
        return this.prescriptionRepository.save(prescription);
    }
    
    @Override
    public void delete(Long code, Connection con) {
        this.prescriptionRepository.delete(code, con);
    }
    
    @Override
    public void delete(Long code) {
        this.prescriptionRepository.delete(code);
    }
    
    @Override
    public void deleteByModel(Prescription prescription, Connection con) {
        this.prescriptionRepository.deleteByModel(prescription, con);
    }
    
    @Override
    public void deleteByModel(Prescription prescription) {
        this.prescriptionRepository.deleteByModel(prescription);
    }
}
