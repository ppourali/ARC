/**
 * This file was firstly auto-generated by EWA: 
 * https://marco-sulla.github.io/ewa/
 */

package com.arc.service;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.sql2o.Connection;

import com.arc.aggregator.InterviewAggregator;
import com.arc.model.Interview;
import com.arc.repository.InterviewRepository;


@Service
public class InterviewServiceImpl implements InterviewService {
    @Autowired
    private InterviewRepository interviewRepository;
    
    private InterviewAggregator enrich(Interview interview) {
        InterviewAggregator interviewAggregator = null;
        
        if (interview != null) {
            interviewAggregator = new InterviewAggregator();
            
            interviewAggregator.setInterview(interview);
        }
        
        return interviewAggregator;
    }
    
    private List<InterviewAggregator> enrich(List<Interview> interviews) {
        final List<InterviewAggregator> interviewAggregators = new ArrayList<>();
        
        if (interviews != null) {
            for (Interview interview: interviews) {
                interviewAggregators.add(this.enrich(interview));
            }
        }
        
        return interviewAggregators;
    }
    
    @Override
    public List<InterviewAggregator> findAll(List<String> fields_to_ignore, Connection con) {
        final List<Interview> interviews = this.interviewRepository.findAll(fields_to_ignore, con);
        
        return this.enrich(interviews);
    }
    
    @Override
    public List<InterviewAggregator> findAll(Connection con) {
        final List<Interview> interviews = this.interviewRepository.findAll(con);
        
        return this.enrich(interviews);
    }
    
    @Override
    public List<InterviewAggregator> findAll(List<String> fields_to_ignore) {
        final List<Interview> interviews = this.interviewRepository.findAll(fields_to_ignore);
        
        return this.enrich(interviews);
    }
    
    @Override
    public List<InterviewAggregator> findAll() {
        final List<Interview> interviews = this.interviewRepository.findAll();
        
        return this.enrich(interviews);
    }
    
    @Override
    public List<InterviewAggregator> findByModel(Interview interview, List<String> fields_to_ignore, Connection con) {
        final List<Interview> interviews = this.interviewRepository.findByModel(interview, fields_to_ignore, con);
        
        return this.enrich(interviews);
    }
    
    @Override
    public List<InterviewAggregator> findByModel(Interview interview, Connection con) {
        final List<Interview> interviews = this.interviewRepository.findByModel(interview, con);
        
        return this.enrich(interviews);
    }
    
    @Override
    public List<InterviewAggregator> findByModel(Interview interview, List<String> fields_to_ignore) {
        final List<Interview> interviews = this.interviewRepository.findByModel(interview, fields_to_ignore);
        
        return this.enrich(interviews);
    }
    
    @Override
    public List<InterviewAggregator> findByModel(Interview interview) {
        final List<Interview> interviews = this.interviewRepository.findByModel(interview);
        
        return this.enrich(interviews);
    }
    
    @Override
    public InterviewAggregator findByCode(Long code, List<String> fields_to_ignore, Connection con) {
        final Interview interview = this.interviewRepository.findByCode(code, fields_to_ignore, con);
        
        return this.enrich(interview);
    }
    
    @Override
    public InterviewAggregator findByCode(Long code, Connection con) {
        final Interview interview = this.interviewRepository.findByCode(code, con);
        
        return this.enrich(interview);
    }
    
    @Override
    public InterviewAggregator findByCode(Long code, List<String> fields_to_ignore) {
        final Interview interview = this.interviewRepository.findByCode(code, fields_to_ignore);
        
        return this.enrich(interview);
    }
    
    @Override
    public InterviewAggregator findByCode(Long code) {
        final Interview interview = this.interviewRepository.findByCode(code);
        
        return this.enrich(interview);
    }
    
    @Override
    public Interview insert(Interview interview, Connection con) {
        return this.interviewRepository.insert(interview, con);
    }
    
    @Override
    public Interview insert(Interview interview) {
        return this.interviewRepository.insert(interview);
    }
    
    @Override
    public Interview update(Interview interview, boolean exclude_nulls, Connection con) {
        return this.interviewRepository.update(interview, exclude_nulls, con);
    }
    
    @Override
    public Interview update(Interview interview, boolean exclude_nulls) {
        return this.interviewRepository.update(interview, exclude_nulls);
    }
    
    @Override
    public Interview save(Interview interview, Connection con) {
        return this.interviewRepository.save(interview, con);
    }
    
    @Override
    public Interview save(Interview interview) {
        return this.interviewRepository.save(interview);
    }
    
    @Override
    public void delete(Long code, Connection con) {
        this.interviewRepository.delete(code, con);
    }
    
    @Override
    public void delete(Long code) {
        this.interviewRepository.delete(code);
    }
    
    @Override
    public void deleteByModel(Interview interview, Connection con) {
        this.interviewRepository.deleteByModel(interview, con);
    }
    
    @Override
    public void deleteByModel(Interview interview) {
        this.interviewRepository.deleteByModel(interview);
    }
}
